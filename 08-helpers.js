const tech = 'Node.js';
const array = ['Node.js', 'React', 'TypeScript'];


const includesString = tech.includes('Node');
const includesArray = array.includes('');

const startsWith = tech.startsWith('no');
const endsWith = tech.endsWith('s');
console.log({endsWith});